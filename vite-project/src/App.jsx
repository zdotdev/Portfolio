function App() {


  return (
    <h1>Test</h1>
    To do so, execute the “$ git config –global user. <name/password>” command. After the verification, run the “$ git config –global user.name <user-name>” command for username and the “$ git config –global user. password <user-password>” command for password configuration.
  )
}

export default App
